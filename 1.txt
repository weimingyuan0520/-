你现在是一个资深的嵌入式系统工程师和上位机开发专家。我需要你帮我完成一个软硬件一体化的期末综合课程设计项目。

我的工程根目录下有一个名为 `components` 的文件夹，里面包含了现有的硬件驱动程序。

请你严格按照以下四个阶段执行任务。在完成每个阶段后，向我汇报，等待我确认后再进入下一阶段。请自主阅读目录和文件内容来获取你需要的信息。

### 阶段一：驱动分析与系统架构设计
1. 请先读取并分析 `components` 文件夹下的所有驱动文件（重点关注光敏传感器、蜂鸣器、按键、LCD液晶屏、串口、WiFi模块的驱动接口）。
2. 根据驱动 API，结合以下「五大核心模块」需求，设计主程序（如 `main.c` 或相关主逻辑文件）的整体架构和状态机，并生成一份简要的 Markdown 格式的系统流程说明。

### 阶段二：嵌入式设备端（下位机）代码生成
请基于你分析的 `components` 驱动，编写嵌入式设备端的主逻辑代码，必须满足以下所有细节：
1. **传感器数据采集模块**：调用光敏传感器驱动，设备上电后自动采集数据，并在代码中实现简单的数字滤波（如滑动平均滤波），确保数值稳定，剔除异常跳变。
2. **智能控制决策与执行模块**：基于光敏数据设置“多级自定义阈值”。当光照低于/高于不同阈值时，蜂鸣器呈现不同的响应频率或模式（实现分级智能控制）。该阈值必须支持本地按键修改和远程网络修改。
3. **多元化人机交互模块**：
   - **按键**：实现按键消抖。设定按键功能：短按切换 LCD 显示页面/模式，长按进入阈值调整模式。
   - **LCD显示**：设计精美的分区布局。分为：实时数据区（光照值）、系统状态区（蜂鸣器状态、当前阈值）、网络通信区（WiFi连接状态、IP地址）。
4. **串口双向通信模块**：
   - 上行：按固定格式（如 JSON 或带特定报头的字符串）实时上传光敏数据、蜂鸣器状态、报警信息。
   - 下行：解析串口指令，支持通过串口指令修改光照阈值、启停蜂鸣器。
5. **网络通信模块（方案一）**：
   - 将设备配置为 WiFi Client (STA模式) 连接局域网，并建立 TCP Server 或 TCP Client。
   - 实时将第4点的 JSON 数据通过网络发送给 PC 端。并能接收来自网络的阈值修改、控制指令。

### 阶段三：PC端上位机（Python）开发
请在根目录下新建一个名为 `pc_software` 的文件夹，并使用 Python（推荐使用 PyQt5/PySide6 或 CustomTkinter 结合 PyQtGraph/Matplotlib）编写一个美观的上位机软件：
1. **网络与串口双重连接**：支持通过 TCP/UDP Socket 连接嵌入式设备的 IP，同时也保留串口连接的选项。
2. **可视化界面美化设计**：
   - 布局规整，深色/浅色现代主题。
   - **数据区**：大字体实时刷新当前光敏数值。
   - **曲线区**：动态绘制光敏传感器历史数据的实时曲线图。
   - **控制区**：提供直观的滑动条或输入框下发指令，修改设备端的光照阈值，以及直接启停蜂鸣器的按钮。
   - **日志区**：滚动显示收发的数据报文和设备报警信息，支持将日志保存为本地 TXT 或 CSV 文件。
3. 提供完整的 `requirements.txt` 以便我安装依赖。

### 阶段四：工程检查与文档配合
检查嵌入式端和上位机端的数据协议（如 JSON 字段名）是否严格对齐。确认无误后，告诉我代码结构，以便我后续按照《课程设计要求》撰写文档。

现在，请开始执行【阶段一】，读取 `components` 文件夹并给我架构方案。